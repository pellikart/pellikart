VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-publishable-key
# Optional. Digits only, with country code (e.g. 919876543210). Shown as the
# WhatsApp fallback if an expert-call request can't be saved. Unset = no fallback.
VITE_PELLIKART_WHATSAPP=
